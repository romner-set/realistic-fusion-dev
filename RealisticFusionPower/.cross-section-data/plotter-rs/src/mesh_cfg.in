c.configure_mesh()
    .x_labels(5)
    .y_labels(12)
    .x_label_formatter(&format_func)
    .y_label_formatter(&format_func)
    .x_label_style(("arial", (width*0.025) as u32))
    .y_label_style(("arial", (width*0.025) as u32))
    //.x_label_offset((width*0.005) as u32)
    .y_label_offset(-(width*0.003) as i32)
    .x_desc("Energy in centre-of-mass frame [keV]")
    .y_desc("σ [barn]")
    .draw()